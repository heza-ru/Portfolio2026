# Canonical site URL (no trailing slash). Used for SEO meta, sitemap, and robots.txt.
# Example: https://your-domain.com
# On Cloudflare: set this in the dashboard (Pages → Settings → Environment variables,
# or Workers secrets/vars) to your *.pages.dev URL or custom domain before building.
VITE_SITE_URL=https://haider.digital
